import 'package:flutter/material.dart';

class ImportantNotices extends StatelessWidget {
  const ImportantNotices({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Important Notices'),
    );
  }
}
